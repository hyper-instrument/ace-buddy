---
description: Show ace-buddy daemon and device status at a glance.
---

Show the ace-buddy daemon and device status by running:

```bash
bash "$CLAUDE_PLUGIN_ROOT/scripts/status.sh"
```

This prints whether the daemon is running, which serial port (if any) is present, whether hooks are installed, and the last few daemon log lines.
